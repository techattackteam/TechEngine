---
description: Draft an Architecture Decision Record for a load-bearing decision
argument-hint: "<the decision or question>"
---

Help me write an ADR for TechEngine.

Decision / question: $ARGUMENTS

Steps:
1. Read `docs/03 Architecture/ADR Index.md` to get the next ADR number and see
   related ADRs. Read any relevant code so the ADR is grounded in reality.
2. Act as technical lead: lay out the real options with honest trade-offs. If
   there's evidence in the codebase, cite it (file:line). Do not rubber-stamp my
   preferred option — argue the alternatives fairly and give a recommendation.
3. Draft the ADR from `docs/Templates/ADR Template.md` as
   `docs/03 Architecture/ADR-NNN — <title>.md` with Status: Proposed.
4. Add it to `docs/03 Architecture/ADR Index.md`.
5. Tell me what evidence would move the decision, so I can Accept it deliberately
   rather than by default.
