#include "common/include/components/audio/Listener.hpp"

namespace TechEngineAPI {
    Listener::Listener(Entity entity) : Component(entity) {
        // Constructor implementation can be empty or contain initialization logic if needed
    }

    void Listener::updateInternalPointer(TechEngine::Scene* scene) {
        
    }
}
