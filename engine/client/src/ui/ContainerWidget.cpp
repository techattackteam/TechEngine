#include "ContainerWidget.hpp"

namespace TechEngine {
    ContainerWidget::ContainerWidget() {
        m_name = "Container";
        m_category = "Container";
        m_description = "A simple container widget that can contain other widgets";
    }
}
